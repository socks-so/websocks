# Quickstart

```js
// Install the package
const main() {
  const { createClient } = require('node-graphql-client');

  const client = createClient({
    url: 'https://api.github.com/graphql',
    headers: {
}
```
